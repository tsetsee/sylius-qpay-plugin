{% extends '@SyliusShop/layout.html.twig' %}

{% import '@SyliusUi/Macro/messages.html.twig' as messages %}

{% block title %}{{ 'sylius.ui.qpay_payment'|trans }}{% endblock %}

{% block content %}
    <div class="ui segment">

        {% set payment = order.lastPayment('processing') %}

        {% if payment %}
            {% include '@TsetseeSyliusQpayPlugin/qr.html.twig' with payment.details %}
        {% endif%}
        {#
        {% if order.paymentState in ['awaiting_payment'] %}
        {{ render(controller('Tsetsee\\SyliusQpayPlugin\\Controller\\QPayQRController::renderQRAction', {id: order.id })) }}
        #}
    </div>
{% endblock %}
